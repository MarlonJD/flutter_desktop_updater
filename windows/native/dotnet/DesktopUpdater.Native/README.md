# DesktopUpdater.Native

`DesktopUpdater.Native` provides the Windows staged-artifact install helper and
its versioned C ABI wrapper for .NET consumers. Applications remain responsible
for discovering, downloading, and verifying update artifacts before calling the
helper.
